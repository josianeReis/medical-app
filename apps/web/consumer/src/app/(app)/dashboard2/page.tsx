"use client";
import CreateReport from "../create-report/page";

export default function DashboardContent() {
  // const { state } = useSidebar();

  // // Responsivo: ajusta a margem para diferentes estados da sidebar
  // const sidebarInsetMargin = state === "collapsed" ? "ml-[1rem]" : "ml-[8rem]";

  return <CreateReport />;
}
